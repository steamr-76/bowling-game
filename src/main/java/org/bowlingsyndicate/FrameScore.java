package org.bowlingsyndicate;

public enum FrameScore {
    OpenFrame,  // <-- two throws, but still pins standing
    Spare,      // <-- second throw has no pins standing
    Strike      // <-- First throw drops all pins.
}
